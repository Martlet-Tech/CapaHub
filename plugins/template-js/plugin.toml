[plugin]
name = "template-js"
version = "0.1.0"
description = "JS plugin template — mouse tracking"

[hooks]
mouse = true

[events]
subscribes = ["mouse.down", "mouse.move", "mouse.up"]
publishes = []
